# LLM Provider Configuration
# Set one of these API keys (in order of preference: Anthropic > OpenAI > OpenRouter)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
OPENROUTER_API_KEY=

# Optional: Override the default provider and model
# LLM_PROVIDER=openai  # Options: anthropic, openai, openrouter
# LLM_MODEL=gpt-4o     # Override the default model

# Search API (required for web search)
SERPAPI_API_KEY=

# Server Configuration
PORT=8787
# SQLITE_PATH=../data/notes.db  # Optional: custom database path
